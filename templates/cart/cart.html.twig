{% extends 'base.html.twig' %}

{% block title %}Hello CartController!{% endblock %}

{% block body %}
    <main class="container">
        <section class="row">
            <div lass="col-md-12">
                <h1>Payement :D</h1>

                <table class="table">
                    <thead>
                        <tr>
                            <th>Produit</th>
                            <th>Prix</th>
                            <th>Quantité</th>
                            <th>Total</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for element in data %}
                            <tr>
                                <td>{{element.article.title}}</td>
                                <td>{{element.article.price}}</td>
                                <td>{{element.quantity}}</td>
                                <td>{{element.quantity * element.article.price}}</td>
                                <td>
                                    <a href="{{ path('app_cart', {id: element.article.id}) }}" class="btn btn-danger">+</a>
                                    <a href="{{ path('app_remove', {id: element.article.id}) }}" class="btn btn-danger">-</a>
                                    <a href="{{ path('app_delete', {id: element.article.id}) }}" class="btn btn-danger">suppr</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="3">Total</td>
                            <td>{{ total }}</td>
                            <td>
                                <a href="{{ path('app_empty') }}" class="btn btn-danger">EFFACER</a>
                            </td>
                        </tr>
                    </tfoot>
                </table>
                
                <a href="{{ path('app_payement') }}" class="btn btn-danger">Payer</a>

            </div>
        </section>
    </main>
{% endblock %}
